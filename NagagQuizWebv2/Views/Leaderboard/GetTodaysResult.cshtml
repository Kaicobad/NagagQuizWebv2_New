@using NagagQuizWebv2.Viewmodels
@{
    var leaderData = new List<TopResult>();
    leaderData = ViewBag.data;
}
<style>
    .mainDesign{
        padding-top: 5px;
        padding-bottom: 3px;
        margin: auto;
        color: rgba(102, 102, 102, 1);
        font-size: 12px;
    }
</style>

<div class="card-body" style="padding-left: 8px;padding-top: 0px;">

    @foreach (var item in leaderData)
    {
        <a style="text-decoration: none;">
            <div class="row boxBasic">
                <div class="col-6 mainDesign tobng" style="text-align:left; ">
                    @item.msisdn
                </div>
                <div class="col-3 mainDesign tobng" style="text-align: right;">
                    @item.timeInSeconds সে:
                </div>
                <div class="col-3 mainDesign" style="text-align:right;">
                   <span class="tobng" style="padding:5px">@item.score</span>
                </div>
            </div>
        </a>
    }
</div>